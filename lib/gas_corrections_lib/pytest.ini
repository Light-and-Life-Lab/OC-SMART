[pytest]
# h5py uses the deprecated function 'product', which gives numerous warnings
# Nothing we can do about it, since it's internal to h5py, so suppress the warnings to reduce noise
filterwarnings = ignore::DeprecationWarning