class Config:
    # Shared configuration constants for OC-SMART.
    # Currently only array datatype, add any other constants shared across files here

    # Datatype for the pre-allocated arrays used in OCSMART.py and mlnn.py, set to float32 to save memory
    datatype = 'float32'
